# Host Caddy config for Docker deployment.
# The frontend container listens on 127.0.0.1:18080.
ai.geoxiangliang.com {
	encode gzip zstd

	reverse_proxy 127.0.0.1:18080

	# Optional: proxy API requests to backend.
	# reverse_proxy /api/* 127.0.0.1:8080
}
